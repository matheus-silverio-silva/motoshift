[phases.setup]
nixPkgs = ["flutter", "dart", "python3"]

[phases.install]
cmds = ["flutter pub get"]

[phases.build]
cmds = [
  "flutter build web --dart-define=API_URL=$API_URL --release"
]

[start]
cmd = "python3 -m http.server $PORT --directory build/web"
