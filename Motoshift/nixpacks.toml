[phases.setup]
nixPkgs = ["flutter"]

[phases.install]
cmds = ["flutter pub get"]

[phases.build]
cmds = ["flutter build web --release --dart-define=API_URL=$API_URL"]

[start]
cmd = "python3 -m http.server $PORT --directory build/web"
